[project]
name = "opentrons-driver"
version = "0.1.0"
description = "High-level Opentrons OT-2 wrapper used by the edge service"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.12.5",
    "requests>=2.32.0",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["opentrons_driver*"]
